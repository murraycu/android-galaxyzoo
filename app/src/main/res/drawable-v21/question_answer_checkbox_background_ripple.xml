<?xml version="1.0" encoding="utf-8"?>
<selector
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Strangely, we need to use android:drawable, not android:color even when specifying
    a color. -->
    <item android:state_checked="true"
        android:drawable="@drawable/question_answer_checkbox_checked_ripple" />

    <!-- Default State -->
    <item android:drawable="@drawable/question_answer_checkbox_ripple" />
</selector>